# Next.js (Vercel) server-side API route key for /api/game/score
OPENAI_API_KEY=
# Optional: override the responses model (defaults to gpt-5-nano)
OPENAI_MODEL=gpt-5-nano

# PartyKit websocket host used by browser clients
# Local dev default: 127.0.0.1:1999
# Example deployed value: jeoprompty.<your-partykit-account>.partykit.dev
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999
